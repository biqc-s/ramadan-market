# 🚀 دليل رفع وتشغيل بوت السوق الرمضاني

يشرح هذا الدليل كيفية رفع البوت وتشغيله ليعمل 24 ساعة باستخدام **GitHub** و **PythonAnywhere**.

---

## 📸 1. إعداد الصور (Cloudinary)
بما أن البوت يعمل على سيرفر سحابي، لا يمكننا حفظ الصور محلياً. نستخدم **Cloudinary** (مجاني) لاستضافة الصور.

1.  سجل حساب مجاني في [cloudinary.com](https://cloudinary.com/).
2.  احتفظ بالبيانات التالية (سنحتاجها لاحقاً في الخطوة 4):
    -   `Cloud Name`
    -   `API Key`
    -   `API Secret`

---

## 🐙 2. رفع الكود إلى GitHub
يجب أولاً رفع الكود الخاص بك إلى موقع GitHub ليسهل نقله إلى السيرفر.

### أ. إنشاء المستودع (Repository)
1.  اذهب إلى [GitHub.com](https://github.com/new) وسجل دخولك.
2.  أنشئ مستودعاً جديداً (New Repository).
3.  في خانة الاسم اكتب: `ramadan-market`.
4.  اجعله **Public** (عام) أو **Private** (خاص) كما تحب.
5.  ⛔ **تنبيه:** لا تضع علامة صح ❌ على "Add a README file" أو تنشئ `.gitignore` من هناك.
6.  اضغط **Create repository**.

### ب. رفع الملفات من جهازك
6.  (غير مهم الآن لأنك ربطت الدومين `https://rijal.marct.i3j.io`).

نفذ هذه الأوامر في التيرمينال لرفع الملفات:

```bash
# استبدل USERNAME باسم المستخدم الخاص بك في GitHub
git add .
git commit -m "تأمين البوت: استخدام ملف .env"
git push -u origin main
```

---

## 🐍 3. التشغيل على سيرفر PythonAnywhere (الأفضل للبوتات)

1.  سجل حساب في [pythonanywhere.com](https://www.pythonanywhere.com/).
2.  اذهب إلى تبويب **Consoles**.
3.  اضغط على **Bash** لفتح شاشة أوامر جديدة.
4.  اكتب الأوامر التالية بالترتيب:

### خطوة 1: سحب الكود
```bash
# انسخ مشروعك من جيت هب
git clone https://github.com/USERNAME/ramadan-market.git
```

### خطوة 2: إعداد الرموز السرية (هام جداً!) 🔐
بما أننا حذفنا الرموز من الكود لحمايته، يجب أن ننشئ ملف `.env` على السيرفر.
في نفس شاشة الـ Bash، اكتب هذا الأمر لإنشاء الملف وفتحه:

```bash
cd ramadan-market
nano .env
```
**(ستفتح لك شاشة تعديل نصوص، انسخ والصق البيانات التالية فيها)**:
```text
TELEGRAM_TOKEN=8158433190:AAGdPs26bOZ1dvhkG4cq27xs6oOp0iW7ZYk
CLOUDINARY_CLOUD_NAME=duyt3dzdz
CLOUDINARY_API_KEY=647178117181471
CLOUDINARY_API_SECRET=cSkbTEQocZBBtlTawfApbAgi7To
```
*   للخروج والحفظ: اضغط `Ctrl + X` ثم `Y` ثم `Enter`.

### خطوة 3: تثبيت المكتبات وتشغيل البوت
```bash
# تثبيت المكتبات (بما فيها python-dotenv)
pip install --user -r requirements.txt

# تشغيل البوت
python bot.py
```

🎉 **مبروك! البوت يعمل الآن بأمان.**

---

## 🌐 4. تشغيل الموقع (المنيو)
لتشغيل صفحة المنيو `index.html`:

1.  في صفحة مستودعك على **GitHub**.
2.  اذهب إلى **Settings** > **Pages**.
3.  اختر الفرع `main` والمجلد `/root` ثم **Save**.
4.  انسخ رابط الموقع الناتج.
